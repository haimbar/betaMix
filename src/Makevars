# Enable 64-bit file offsets on 32-bit Linux/POSIX (fseeko uses off_t).
# On 64-bit systems this is a no-op; on Windows _fseeki64 is used directly.
PKG_CPPFLAGS = -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
